# Freeze snapshot

Pre-launch freeze created **before** public-readiness work.

| Item | Value |
|------|-------|
| Date | 2026-07-09 |
| Git tag | `v1.0.0-pre-launch-freeze` |
| Commit | `b68e88c` (or `git rev-list -n1 v1.0.0-pre-launch-freeze`) |
| Tarball | `../systemic-tau-platform-freeze-prelaunch-20260709-153807.tar.gz` |

## Restore

```bash
git checkout v1.0.0-pre-launch-freeze
# or extract the sibling tarball into a clean directory
```

The public-ready release is tagged `v1.0.0` on `main`.
